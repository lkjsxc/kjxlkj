# Branding and Icon Contract

## Brand Mark

- The shell brand remains `kjxlkj`.
- The product also uses one compact icon mark derived from the reference artwork in `tmp`.
- The icon mark uses the same blue-cyan palette family as the provided references.
- The icon text is exactly `kjx`.
- The icon frame is circular.
- The `kjx` letters are visually larger and centered for small-scale legibility.

## Rendering Rules

- The editable icon source of truth is an SVG asset.
- Production favicon delivery uses `favicon.ico`.
- The SVG must render cleanly at favicon scale and at small shell-brand sizes.
- The icon may use a bold sans-serif treatment sized to stay legible at small scales.
- The HTML document head links to the `.ico` favicon.
- The shell brand may pair the icon with the `kjxlkj` wordmark in desktop and compact headers.
- Verification must check both the in-shell SVG rendering and the head-linked `.ico` asset.
