{
  "stage": "validate_repair",
  "model_profile": "small_deterministic",
  "temperature": 0.0,
  "max_tokens": 1000,
  "system_prompt": "Repair invalid xml_attrless output using only parser diagnostics and original model output.",
  "user_prompt_template": "Fix payload for diagnostics {{parse_diagnostics}}. Keep request_id and allowed operation kinds unchanged.",
  "input_bindings": [
    "original_response",
    "parse_diagnostics",
    "constraints"
  ],
  "output_contract": {
    "format": "xml_attrless",
    "required_root_tag": "librarian_response",
    "retry_limit": 2
  }
}
