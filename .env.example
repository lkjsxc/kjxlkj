# Secrets — per /docs/spec/architecture/configuration.md.
# All non-secret configuration belongs in data/config.json.
# Only sensitive credentials go here.
# Copy this file to .env and set real secret values.
# Secrets MUST stay in .env and MUST NOT be committed.

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://postgres@localhost/kjxlkj

# OpenRouter API key (optional, only for openrouter provider)
# OPENROUTER_API_KEY=sk-...
