# Verification stage Dockerfile
FROM rust:1.91-alpine

WORKDIR /app

RUN apk add --no-cache build-base musl-dev
RUN rustup component add rustfmt clippy

COPY verify.sh /verify.sh
RUN chmod +x /verify.sh

ENTRYPOINT ["/verify.sh"]
