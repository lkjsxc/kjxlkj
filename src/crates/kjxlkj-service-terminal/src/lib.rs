//! Terminal/PTY service (placeholder).
//!
//! Will provide integrated terminal panes.

/// Terminal service state.
pub struct TerminalService {
    // Placeholder
}

impl TerminalService {
    /// Create a new Terminal service.
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for TerminalService {
    fn default() -> Self {
        Self::new()
    }
}
