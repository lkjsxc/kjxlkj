//! Index/navigation service (placeholder).
//!
//! Will provide file indexing and fuzzy finding.

/// Index service state.
pub struct IndexService {
    // Placeholder
}

impl IndexService {
    /// Create a new Index service.
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for IndexService {
    fn default() -> Self {
        Self::new()
    }
}
