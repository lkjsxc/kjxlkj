//! Index service.
//!
//! File indexing and navigation (placeholder).

/// Placeholder index service.
pub struct IndexService;

impl IndexService {
    /// Creates a new index service.
    pub fn new() -> Self {
        Self
    }
}

impl Default for IndexService {
    fn default() -> Self {
        Self::new()
    }
}
