//! LSP service.
//!
//! Language Server Protocol client (placeholder).

/// Placeholder LSP service.
pub struct LspService;

impl LspService {
    /// Creates a new LSP service.
    pub fn new() -> Self {
        Self
    }
}

impl Default for LspService {
    fn default() -> Self {
        Self::new()
    }
}
