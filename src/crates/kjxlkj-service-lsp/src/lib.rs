//! LSP client service (placeholder).
//!
//! Will provide language server protocol integration.

/// LSP service state.
pub struct LspService {
    // Placeholder
}

impl LspService {
    /// Create a new LSP service.
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for LspService {
    fn default() -> Self {
        Self::new()
    }
}
