//! LSP service (placeholder).
//!
//! Language Server Protocol client service.

/// LSP service placeholder.
pub struct LspService;

impl LspService {
    /// Create a new LSP service.
    pub fn new() -> Self {
        Self
    }
}

impl Default for LspService {
    fn default() -> Self {
        Self::new()
    }
}
